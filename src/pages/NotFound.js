import React from "react";
import Layout from "../components/Layout";

const NotFound = () => (
  <Layout>
    <h1 className="mb-4 text-xl">We couldn't find that</h1>
    <p className="text-lg">
      The link you've followed doesn't exist ... or does it?
    </p>
  </Layout>
);

export default NotFound;
